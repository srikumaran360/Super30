<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Anton&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="./css/signup.css">
    <title>Signup</title>
</head>
<body>
    <h1>SUPER 30</h1>
    <h3>SIGN UP</h3>
    <div class="container">
        <div class="child">
            <form id="form" action="/signup" method="POST">
                <label for="username">Username</label>
                <input type="text" name="username" placeholder="Enter a username" required="true">
                <label for="email">Email</label>
                <input type="email" name="email" placeholder="Enter your email address" required="true">
                <!-- <% if(err){ %>
                <p><%= err %></p>
                <% } %> -->
                <label for="password">Password</label>
                <input type="password" name="password" minlength="8" placeholder="Create a Password" required="true"> 
                <button type="submit">submit</button>
            </form>
        </div>
    </div>
    <h5><a href="/login">Click here</a> to Login</h5>
    <div id="notification" class="toast">
        <%=err%>
    </div>
    <script>
        // let counter = 3;
        // let id;
        // function print()
        // {
        //     id = setInterval(callback,1000);
        // }
        // function callback()
        // {
        //     console.log(counter);
        //     counter--;
        //     if(counter <= 0)
        //         clearInterval(id);
        // }
        
        let element = document.getElementById("notification");
        if("<%=err%>" && "<%=err%>" != "verification email sent")
        {
            element.style.visibility = "visible";
        setTimeout(() => {
            element.style.visibility = "hidden";
        }, 3000);
        }
        if("<%=err%>" === "verification email sent")
        {
            element.style.backgroundColor = "green";
            element.style.borderColor = "green";
           element.style.visibility = "visible";
           // print();
        setTimeout(() => {
            location.href = "http://localhost:3000/login";
        }, 3000);
        }
    //     async function validate(){
    //     let form = document.getElementById("form");
    //     fetch("http://localhost:3000/signup",{
    //         method:"POST",
    //         headers:{
    //             'Content-Type': 'application/json'
    //         },
    //         body: JSON.stringify(form)
    //     }).then(res => {
    //         return res.json();
    //     })
    //     .then(data => {
    //     element.innerText = data.message;
    //     
    //     setTimeout(() => {
    //         element.style.visibility = "hidden";
    //     }, 3000);
    //     })
    //     .catch(error => console.log(error))
        
    // }
    </script>
</body>
</html>